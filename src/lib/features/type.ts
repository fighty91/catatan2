export interface Contact {
  id: string;
  name: string;
  [key: string]: any; // Untuk properti tambahan lainnya
}

export interface Product {
  id: string;
  name: string;
  [key: string]: any; // Untuk properti tambahan lainnya
}